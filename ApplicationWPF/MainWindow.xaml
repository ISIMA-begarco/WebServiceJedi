<Window x:Class="ApplicationWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="Frames/IFrameNavigator"
        Loaded="WindowLoaded"
    Title="Jedi Tournament" WindowState="Maximized" WindowStyle="None" ResizeMode="NoResize" Background="Black">
    <Grid>
        <!-- Content="{Binding Path=CurrentFrame, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" -->
        <Frame x:Name="MainFrame" Grid.Row="1" Padding="5" NavigationUIVisibility="Hidden" />
    </Grid>
</Window>
